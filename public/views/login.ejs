<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="style.css">
    <title>Login Page</title>
</head>
<body>
   <% if (loginerror === true) { %>
    <script>
        alert('Username or Password is incorrect');
    </script>
    <% } %>
    <div class="min-h-screen flex justify-center items-center bg-gray-50">
        <div class="w-full max-w-md">
            <div class="rounded-lg border bg-white text-[#222] shadow-sm">
                <div class="flex flex-col pt-6">
                    <h3 class="whitespace-nowrap tracking-tight text-2xl font-semibold text-center">Login</h3>
                </div>
                <div class="p-6">
                    <form action="/login" method="post" class="space-y-4">
                        <div class="space-y-2">
                            <label for="username" class="text-sm font-medium leading-none">Username</label>
                            <input type="text" name="username" id="username" class="flex h-10 w-full rounded-md
                            border border-gray-100 bg-gray-50 px-3 py-2 text-sm focus:outline-none">
                        </div>
                        <div class="space-y-2">
                            <label for="password" class="text-sm font-medium leading-none">Password</label>
                            <input type="password" name="password" id="password" class="flex h-10 w-full rounded-md
                            border border-gray-100 bg-gray-50 px-3 py-2 text-sm focus:outline-none">
                        </div>
                        <div class="space-y-2">
                            <input type="submit" value="login" class="w-full h-10 rounded-md border bg-[#222]
                            text-white hover:shadow-lg hover:text-[#c2df84] focus:outline-none cursor-pointer transition-all">
                        </div>
                        <div class="flex justify-between items-center">
                            <a href="/register" class="text-sm font-medium hover:underline transition-all">Register</a>
                            <a href="forgot-password.ejs" class="text-sm font-medium hover:underline transition-all">ลืมรหัสผ่าน?</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
